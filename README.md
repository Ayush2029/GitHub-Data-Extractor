# GitHub-Data-Extractor
A web app that automatically extracts GitHub links from a PDF (like a resume) and generates a rich, dynamic dashboard of profile and repository data. Built with Next.js and Tailwind CSS.
